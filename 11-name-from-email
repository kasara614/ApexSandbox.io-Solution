public String nameFromEmail(String email) { 
    String[] parts = email.toLowerCase().split('@');
    String[] names = parts[0].split('\\.');

    String firstName = names[0].capitalize();
    String lastName = names[1].capitalize();

    return firstName + ' ' + lastName;
}